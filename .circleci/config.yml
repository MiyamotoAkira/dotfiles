version: 2.0

jobs:
  mint:
    docker:
      - image: linuxmintd/mint19.1-amd64
    steps:
      - run: mint/up.sh
      - run: test.sh
  fedora:
    docker:
      - image: fedora:30
    steps:
      - run: fedora/up.sh
      - run: test.sh

workflows:
  version: 2
  mint:
    jobs:
      - mint

  fedora:
    jobs:
      - fedora
